# Root frontend environment template.
# Keep broker credentials out of root/frontend env files.
# Put Kiwoom credentials only in backend/.env for local backend verification.

VITE_API_BASE_URL=http://127.0.0.1:8000
